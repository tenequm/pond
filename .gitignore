# Rust
target/

# IDEs
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Temporary files
*.tmp
*.swp

.env*
tmp/
.claude/*local.json
.claude
.agents/skills/
/benches/corpus/

# Benchmark fixtures/results capture full conversation content from the
# operator's local corpus (including API keys, wallet addresses, and private
# project paths embedded in indexed messages). Regenerate locally via
# `bench/embeddings/run.sh`. Never commit these.
/bench/embeddings/fixtures/
/bench/embeddings/results/
/bench/embeddings/__pycache__/

# Corpus snapshot enumerates the operator's projects by name; aggregate
# stats appear inline in the benchmark report. Regenerate via `pond status`.
/docs/researches/embeddings/snapshot.txt
