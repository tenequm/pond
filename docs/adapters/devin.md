# devin

Last verified: 2026-09-26, against devin 3000.11.3 (commit 9c803229faa4), on macOS and Windows 11.

Format archaeology and the decision record for the `devin` adapter, which reads the local store of Cognition's `devin` CLI (issue [#315](https://github.com/tenequm/pond/issues/315)). The adapter code in `packages/pond/src/adapter/devin.rs` stays authoritative for extraction behavior; this document records facts about the upstream format and the choices made against them.

## Upstream pointers

- Product: the Devin CLI ([docs](https://docs.devin.ai/cli), [stable changelog](https://docs.devin.ai/cli/changelog/stable.md)), installed as the `devin-cli` Homebrew cask on macOS and natively under `%LOCALAPPDATA%\devin\cli` on Windows. The CLI is closed source: there is no writer source to clone and no git history to trace.
- The writer, read from the binary instead: `strings` over `/opt/homebrew/Caskroom/devin-cli/3000.11.3/bin/devin` yields every refinery migration body (`V1__initial_schema` through `V17__subagent_heads`, with their SQL comments) and every statement the session store issues - `INSERT INTO message_nodes (session_id, node_id, parent_node_id, chat_message, created_at, metadata)`, `UPDATE sessions SET main_chain_id = ?1, last_activity_at = ?2 WHERE id = ?3`, `DELETE FROM message_nodes WHERE session_id = ?1 AND node_id BETWEEN ?2 AND ?3` (revert), the per-table `DELETE ... WHERE session_id = ?1` sequence (`devin rm`), `INSERT OR REPLACE INTO tool_call_state`, `INSERT OR REPLACE INTO subagent_heads`, the fork's `INSERT INTO sessions ... SELECT ... FROM sessions WHERE id = ?3`, and the open-time `journal_mode` `WAL` / `synchronous` `NORMAL` / `busy_timeout` pragmas. The binary holds no `UPDATE message_nodes` statement: a node row is never rewritten in place. Every shape below that a capture produced was also checked against the sandboxed captures (see Fixture); the rest is marked as binary-only.
- Official docs, cited below as `CL:<line>` of the stable changelog page (as fetched 2026-09-26) plus the commands, subagents and auth reference pages.
- Third-party reference, non-authoritative: the research notes in issue #315 (pointers only; every claim taken from them was re-checked against the binary or a capture).

## The store of record

One SQLite database per install, `<data>/devin/cli/sessions.db`: `$XDG_DATA_HOME/devin/cli/` (default `~/.local/share/devin/cli/`) on macOS and Linux, `%APPDATA%\devin\cli\` on Windows. WAL mode, schema managed by refinery (`refinery_schema_history`, version 17 at 3000.11.3); the CLI runs the pending migrations on every open. Tables:

| Table | Rows | Payload |
|---|---|---|
| `sessions` | one per session, primary key `id` | `working_directory`, `backend_type`, `model`, `agent_mode`, `created_at` / `last_activity_at` (unix seconds), `title`, `main_chain_id`, `shell_last_seen_index`, `cogs_json`, `workspace_dirs`, `hidden`, `metadata` |
| `message_nodes` | one per node of a session's message forest, `row_id` AUTOINCREMENT, `UNIQUE(session_id, node_id)` | `node_id`, `parent_node_id` (NULL for a root), `chat_message` (JSON), `created_at` (unix seconds), `metadata` (JSON `NodeMetadata`) |
| `tool_call_state` | one per tool call, primary key `(session_id, tool_call_id)` | `tool_call_json` (the ACP `ToolCall`) and `tool_call_update_json` (the final `ToolCallUpdate`); the migration comment says resume replays them "instead of reconstructing from the message forest" |
| `subagent_heads` | one per persistent subagent, primary key `(session_id, agent_id)` | `chain_node_id`, `updated_at`; empty in every capture |
| `prompt_history` | one per typed prompt | `content`, `timestamp`, `session_id`, `is_shell` |
| `rendered_commits` | HTML render cache per session | `sequence_number`, `rendered_html`; empty in every capture |
| `app_state` | global UI key-value | `schema_compat_version` only, in both captures |
| `refinery_schema_history`, `sqlite_sequence` | bookkeeping | |

`message_nodes` holds a forest per session, not a list. Before each model call the harness rebuilds the model context as a new root: the system-prefix messages (node `metadata.is_system_prefix` true) followed by copies of the prior conversation, each copy's node `metadata.extensions."compact/prior_node_ids"` naming the nodes it was copied from. A one-turn `-p` session therefore writes 24-27 nodes for 8-9 distinct messages (`level-waterlily` 27/9, `spotless-lunaria` 24/8), and the whole fixture holds 381 nodes for 172 distinct messages. `sessions.main_chain_id` is the node id of the live head; walking `parent_node_id` from it yields the context exactly as the model last saw it (system prefix included). Everything off that chain - turns before a compaction, the originals of rebuilt copies, the compaction summarizer's own chain, a subagent's chain - stays in the table.

`chat_message.message_id` (a UUID v4) is stable across every copy of a message, and copies are byte-identical with one exception: a later copy of an assistant or tool message may add `metadata.extensions."chisel/tool_call_content"` (ACP tool-call content attached after the fact). The newest copy (highest `row_id`) is therefore a superset of every earlier one - measured: 26 of the fixture's 172 messages have more than one variant, and in all 26 every older variant equals the newest with that one key removed.

`chat_message` keys: `message_id`, `role` (`system` / `user` / `assistant` / `tool`), `content` (always a string), `tool_calls` (`[{id, name, arguments, index, kind: "function"}]`, `arguments` an object), `tool_call_id`, `thinking` (`{thinking, signature}`), `images` (a list; empty in every capture), `phase` (`final_answer`), and `metadata` (`created_at` as RFC 3339 with microseconds, `is_user_input`, `num_tokens`, `request_id`, `metrics`, `finish_reason`, `started_generation_at`, `generation_model`, `response_dimensions`, `telemetry {source, operation}`, `extensions`). Extensions observed: `chisel/terminal_output`, `chisel/tool_call_timing`, `chisel/tool_result_meta {success, kind}`, `chisel/tool_call_content`, `chisel/tool_failure {reason: Canceled | ValidationError}`, `chisel/undo`, `chisel/user_question_answers`, `chisel/conversation_history`, `chisel/client-message-id`, `devin-rs/summary {source: file_compactor}`, `affogato/cog-context`, `agent-ext/skills-loaded`, and the subagent link `subagent/{agent_id, chain_node_id, profile_name, model}`. Node `metadata` keys: `summarized_from`, `num_tokens_preceding`, `is_system_prefix`, `extensions {compact/prior_node_ids}`; some nodes carry the JSON literal `null` instead.

## Decision table

| # | Decision | Answer | Evidence |
|---|----------|--------|----------|
| 1 | `source_agent` brand | `devin`, the binary name. Two kind subpaths: `devin/subagent` for a subagent child (row 7), and `devin/helper` for a `sessions` row with `hidden = 1` - per the V15 migration comment, internal helper agents such as the summarizer persist their sessions hidden, and devin's own lists filter `hidden = 0`. A helper row is kept verbatim like any other; the kind is what keeps it out of a `devin`-scoped search. A `/fork` is a user-visible peer in `devin list` (the fork `INSERT` sets `hidden` 0) and stays on the root brand. | Fixture `chalk-twig` (two background subagent children), `amplified-color` (one foreground child), fork `power-almandine`; no capture produced a hidden row, so `devin/helper` is unit-tested against a fixture row flipped to `hidden = 1`. |
| 2 | Session identity | `sessions.id` verbatim - a word-pair slug `<adjective>-<noun>` since v2026.4.24-1 (CL:669: "New Devin CLI sessions use memorable word-pair IDs"); older installs may hold other shapes, so the id is checked with `validate_path_id` (which rejects `/` and `:`) at ingest rather than assumed. A subagent child is `<session_id>/agent-<agent_id>` (the claude-code child shape; the `/` keeps it out of default search); an `agent_id` that fails `validate_path_id` names no child - its messages stay with the parent and the session reports a typed error. A message is `<pond session id>:<chat_message.message_id>`: the session prefix is what keeps it unique, because a fork copies `message_id`s verbatim. | Fixture ids `level-waterlily`, `viridian-bear`, ...; agent ids `398e395d`, `029b10e6`, `1d6d342f`, `716d0f27`. |
| 3 | Project resolution | `sessions.working_directory` (`NOT NULL` in the schema, so no fallback chain), verbatim - `C:\devin-fixture2\work` on Windows. Resuming from another directory can rewrite it (`UPDATE sessions SET working_directory = ?1, last_activity_at = ?2`, CL:585 offers "switching permanently to your current directory"); additive sync never rewrites a stored session row, so the project is latched at first ingest. | Binary `UPDATE sessions SET working_directory`; every fixture row. |
| 4 | Ordering key | `(chat_message.metadata.created_at, message id)`. `created_at` is the writer's own per-message stamp and survives copying unchanged; fallback when absent is the node's `created_at` column (seconds) of the message's first placement. Every captured message carries `metadata.created_at` (0 missing of 172). A fork copies its parent's messages with their original stamps, so inherited history sorts before the fork's own turns. | All seven fixture sessions; fork `power-almandine`. |
| 5 | Tool-call correlation | `call_id` = the assistant's `tool_calls[].id` = the tool message's `tool_call_id`, verbatim (`call_` plus 24 hex digits). Each call also has a `tool_call_state` row: `tool_call_json` (`acp::ToolCall` `{toolCallId, title, kind, content, rawInput, locations, _meta}`, nullable per its migration comment for an interrupted save) and `tool_call_update_json` (the final `{status, content, _meta}`). The result's tool name is `_meta."cognition.ai/inferenceToolName"`, else the calling assistant's `tool_calls[].name`. `is_failure` = update `status == "failed"` or a `chisel/tool_failure` extension on the tool message. A non-zero exit is `completed` with the exit code in the result text, not a failure. An interrupted call is a tool message `Canceled due to user interrupt` with `chisel/tool_failure {reason: Canceled}`, update `status: failed` and `_meta."cognition.ai/canceled": true`. A background command (over 10 s) returns `Command running in background with ID ...` - a completed call. A result without its call does not occur in the writer. | `branch-candy` (`cat missing.txt`, exit 1, `completed`); `amplified-color` (read of a missing file, `ValidationError`, `failed`; interrupted `sleep 9`); `viridian-bear` (interrupted `Start-Sleep`, `Get-Content missing.txt` exit 1). |
| 6 | Provenance | A `user` message with `metadata.is_user_input == true` is conversational; a `user` message without it is injected (the compaction summarizer's input: `Conversation to summarize`, `Now summarize ...`). Assistant text, thinking and tool calls are conversational. `tool` messages are injected tool results. `system` messages are system messages: the system prompts, `<system_info>`, `<available_skills>`, `<subagent_completion_notification>`, and the post-compaction `You are continuing work...` continuation. `content` is always one string, so there is no fused span to split. | `amplified-color` compaction chain; `chalk-twig` completion notifications. |
| 7 | Lineage | Subagent: the link is parent-side only and lives in the same database. The parent's `run_subagent` tool result and/or `<subagent_completion_notification>` system message carry `metadata.extensions."subagent/agent_id"`, `"subagent/chain_node_id"` (the subagent's head node in the same session forest), `"subagent/profile_name"` and `"subagent/model"`; `subagent_heads` (written by `INSERT OR REPLACE INTO subagent_heads`, empty in every capture) is the writer's own head record, so when it holds a row for an agent that row wins; otherwise the head is the link row with the highest `row_id` (a resumed subagent advances it). Messages are partitioned by claim: the main chain (from `main_chain_id`) claims first, then each subagent head chain; unclaimed message ids (off-chain history) belong to the main session. A child is emitted only when its head chain claims at least one message. The child's parent is the pond session that owns the first message naming the agent (the `run_subagent` result) - the root, or another subagent when a nested one spawned it - and `parent_message_id` is that message's pond id; the child's raw record is that message's `metadata.extensions` (or the `subagent_heads` row when no link message survives), and `options.devin` carries `agent_id` and the head node. The link resolves on first ingest, so there is no cross-tree latch. Fork (`/fork [step]`): a new `sessions` row copied by `INSERT INTO sessions ... SELECT ... FROM sessions WHERE id = ?3` (fresh `created_at` / `last_activity_at`, a new `main_chain_id`, `hidden` 0, `metadata` not copied), the title suffixed ` (fork)`, the chain's nodes copied with their node ids and `message_id`s verbatim, and the `tool_call_state` rows copied. No parent pointer is stored anywhere - every table and every sibling file was searched; only the logs mention it - so a fork's `parent_session_id` is `None`, a declared gap: inferring it from shared message UUIDs would be a guessed link (`model-no-synthesis`). | Children `amplified-color/agent-398e395d`, `chalk-twig/agent-029b10e6`, `chalk-twig/agent-1d6d342f` (interrupted, then resumed to completion), `viridian-bear/agent-716d0f27`; fork `power-almandine` carries the copied `run_subagent` link for `398e395d` but none of its nodes, so it has no child. |
| 8 | Deliberate non-capture | `prompt_history` (the TUI's prompt-recall copy of typed prompts and `!` shell entries); `rendered_commits` (an HTML render cache); `app_state` (global UI flags); `refinery_schema_history` (read only for the version gate, see Field history); and beside the database `transcripts/` (a lossy ATIF export, see below), `summaries/` (the compaction input dump; the summary itself is a message row), `logs/`, `session_locks/`, `plugins/`, `app_state.json`, `installation_id`, `skill_events_spool.lock`, and `credentials.toml`, which is never read. Everything else is captured: every `message_nodes` row present when its message is first ingested survives on that message (`options.devin.nodes` lists every placement `{row_id, node_id, parent_node_id, created_at, metadata}`; the newest `chat_message` variant is the raw record and the older distinct copies ride in `options.devin.variants`); copies written after that first ingest are not added, because additive sync never rewrites a stored message, every `sessions` column rides on the Session options (the raw record is the whole row read with `SELECT *`), the root Session's options carry `devin.tool_call_state` (the rows), `devin.subagent_heads` and `devin.schema_version`, and a child's options carry its agent id and head node. | Row counts in the Fixture census. |
| 9 | Restore face | `restore_unsupported`. Native is rows in one shared, live SQLite database mutated in place (a revert deletes nodes) plus the forest of context-rebuild copies that pond collapses to one message each; a rebuilt database would be a synthesized forest, not the source's. The reason names the alternative: `pond resume <id> --to claude-code` (or any other target). | Rows 7 and 10. |
| 10 | Freshness oracle | The writer is not append-only for nodes. `/revert` deletes node rows (`DELETE FROM message_nodes WHERE session_id = ?1 AND node_id BETWEEN ?2 AND ?3`; measured on `amplified-color`: 9 rows deleted, 0 changed) and `devin rm` deletes the whole session (measured: its rows left `sessions`, `message_nodes`, `prompt_history` and `tool_call_state`). Compaction, resume, fork and subagents only insert (measured: `/compact` deleted 0 rows, changed 0 and added 6; a `-p -r` resume appended nodes only; `/fork` added a new `sessions` row and 11 copied nodes and left the parent untouched); no node `UPDATE` exists. `tool_call_state` is `INSERT OR REPLACE` (the update JSON lands after the call). The `sessions` row is updated in place (`main_chain_id`, `last_activity_at`, `title`, `model`, `agent_mode`, `working_directory`, `cogs_json`, `metadata`, `workspace_dirs`). The watermark is, per pond session, the newest message time over the messages it owns - each message timed by its first placement, exactly as the read stamps it - computed from one lean pass per database that streams the node graph, `message_id`, `created_at` and the subagent extension fields (no content) one session at a time, so an unchanged session gates `Fresh` exactly. `discover` runs that pass and hands its result to the read, so a sync pays for it once. A node whose `chat_message` is not JSON with a string `message_id` leaves only its own session opaque (it re-reads and the read reports the node), never the whole database. Reverted and deleted messages stay in pond (the superset posture of agy and hermes; additive sync never deletes). Residual: a `tool_call_state` update that lands without a new message does not move the watermark. | The binary SQL above (no node `UPDATE`); every operation measured during capture by snapshot diffs of the database, `/revert` on `amplified-color`. |
| 11 | Windows | Native build. The store is `%APPDATA%\devin\cli\sessions.db` (Roaming), matching the documented `%APPDATA%\devin\credentials.toml`; the XDG variables are ignored there (measured during capture: with `XDG_DATA_HOME` and `XDG_CONFIG_HOME` set, devin still wrote `%APPDATA%\devin\cli` and `%APPDATA%\devin\config.json`). `probe_default` checks, in order, `~/.local/share/devin/cli`, `~/AppData/Roaming/devin/cli` and the legacy `~/.local/share/cognition/cli`; a redirected AppData or a relocated `XDG_DATA_HOME` is configured as an explicit `path` (the claude_desktop_app precedent). A root the probe finds may hold no database - devin's installer puts the binary under that same directory - and simply yields zero sessions. `working_directory` is the Windows path verbatim; shell command output keeps its CRLF inside the JSON string, while the `read` tool's line-numbered view of a CRLF file (`crlf.txt`) is LF-normalized by devin itself; the database itself is ordinary UTF-8 SQLite, so no file-level encoding quirk reaches the adapter. `--sandbox` refuses natively (WSL only), and the first run of a fresh home shows an interactive "connect your Git provider" prompt that blocks `-p` until it is answered once in the TUI. | Windows fixture `viridian-bear`, `spotless-lunaria` (cwd `C:\devin-fixture2\work`; CRLF in the `Get-ChildItem` and `Get-Content` results, none in the `crlf.txt` view); `cli_enterprise_devin-auth` docs page. |

## Field history

No git history exists; the embedded refinery migrations are the history. Each version's name and effect:

- V1 `initial_schema`: `sessions(id, working_directory, backend_type, model, permission_mode, created_at, last_activity_at, total_tokens, current_epoch)` and a linear `messages(id, session_id, sequence_number, role, content, tool_call_id, tool_calls_json, timestamp, compaction_epoch)`.
- V2 `add_thinking_column` (`messages.thinking`); V3 `add_prompt_history`; V4 `add_metadata_column` (`messages.metadata`).
- V5 `message_forest`: creates `message_nodes`, drops `sessions.total_tokens` / `current_epoch`, adds `sessions.title` and `main_chain_id`, and ends with `DROP TABLE IF EXISTS messages`. The migration copies no rows from `messages` into `message_nodes`, and no statement in the binary reads `messages`, so the upgrade discards the linear history rather than converting it.
- V6 `add_node_metadata` (`message_nodes.metadata`); V7 `add_shell_context` (`sessions.shell_last_seen_index`); V8 `add_session_cogs` (`sessions.cogs_json`); V9 `add_rendered_commits`; V10 `add_workspace_dirs`; V11 `add_prompt_history_is_shell`; V12 `add_app_state`; V13 `rename_permission_mode_to_agent_mode`; V14 `tool_call_state`; V15 `add_hidden_column`; V16 `add_session_json_metadata` (`sessions.metadata`); V17 `subagent_heads`.

Legacy handling. The CLI migrates on every open, so a database below V5 is one no forest-era CLI has opened, and its `messages` table is its only history. The adapter skips such a database (no `message_nodes`) as `Unsupported`, naming the file, rather than reading it as empty. The reason says the database predates the message forest, which pond does not read, and tells the user to copy the file aside before running a current `devin` on it, because that migration drops the old `messages` table without converting it. No pre-V5 database was available to capture, so the V1 shape is known only from its migration. A version above 17 is read: columns come through `SELECT *`, so a column a later devin adds rides along in the raw record.

Changelog cross-references: word-pair ids CL:669 (v2026.4.24-1); `/revert <step>`, `/steps` and `/fork [step]` CL:781-785 (v2026.4.17-0); pre-compaction steps visible to `/steps`, `/revert` and `/fork` CL:545 (v2026.5.5-0); the resume-from-another-directory prompt CL:585 (v2026.4.30-0); `--export` in ATIF CL:464 (v2026.5.26-0); sessions saved only after the first message CL:453 (v2026.5.26-5); `devin rm` CL:159 (v3000.5.20); the `~/.local/share/cognition/` to `devin/` move with a compatibility symlink left at the old path CL:1172 (v2026.3.20-2).

## Extra format notes

- Sibling files under `<data>/devin/cli/`: `transcripts/<id>.json`, `summaries/history_<hex>.md`, `logs/`, `session_locks/<id>.lock` (the owning pid), `plugins/`, `app_state.json`, `installation_id`, `skill_events_spool.lock`. `credentials.toml` sits one level up, at `<data>/devin/credentials.toml`, per the auth docs. The legacy root `~/.local/share/cognition/cli` is a compatibility symlink after the CL:1172 migration.
- The ATIF transcript (`transcripts/<id>.json`, ATIF v1.7; the `--export` format) is written by the TUI only - a `-p` headless run writes none - and holds only the live main chain (measured during capture: a TUI session's transcript held 11 steps, with no subagent chain and no reverted branch; `-p` runs wrote no transcript at all). It is a lossy projection of `message_nodes`, hence non-capture.
- devin opens the database with `journal_mode=WAL` and leaves the WAL uncheckpointed, so a live store is a `sessions.db` plus a `-wal` holding the recent rows. The adapter opens read-only through SQLite, which reads the WAL; a copy of the `.db` alone would miss rows. The fixture folds the WAL in and stays in WAL mode.
- `sessions.hidden` (V15) marks sessions that user-facing views skip - per its migration comment the REPL `--list`, `--continue`, prefix resolution and the default ACP agent's `session/list` - and internal helper agents (the summarizer is the example) persist their sessions with it set; the binary's list queries filter `hidden = 0`. pond ingests such a row under `devin/helper` (row 1). No capture produced one (all seven captured rows are 0).
- `sessions.cogs_json` is a JSON array of the harness's context modules ("cogs", 17 per captured session, 25-29 KB): each `{source, lifetime, set_system_prefix, append_system_messages, context, footer_messages, user_display, permissions, tool_availability, model}`. The system messages it appends reach the forest as system-prefix copies; the array itself rides on the Session options. `sessions.metadata` (V16) is the serialized `SessionJsonMetadata`, e.g. `{"total_credit_cost":0,"total_acu_cost":0.0}`; a fork starts without it (the fork `INSERT` does not copy the column) until its next turn writes one.
- Headless `-p` and the TUI write the same tables and the same row shapes; the differences are the missing ATIF transcript under `-p`, the `-p` refusal in an untrusted directory (commands reference), and the Windows first-run Git-provider prompt that blocks `-p`. `--sandbox` makes `autonomous` the only permission mode (essential-commands page).
- Two operations persist nothing (measured during capture): `/btw` runs a side question as a sidechain that is never added to the conversation (commands reference) and wrote no `message_nodes` row and no new session; a TUI started and closed with `/exit` before any message wrote no `sessions` row (CL:453: sessions are saved once the first message is sent).
- `devin rm <session>` (CL:159) is a hard delete: `DELETE FROM` `subagent_heads`, `tool_call_state`, `rendered_commits`, `message_nodes`, `prompt_history` and finally `sessions`, with no trash or soft-delete, refused only while another devin instance holds the session open. `/revert` deletes nodes the same way. Measured during capture: `devin rm` removed the session's rows from `sessions`, `message_nodes`, `prompt_history` and `tool_call_state`. After either, the removed history exists only where something copied it first - which is what this adapter is for.
- The ACP lane (Zed, JetBrains, Xcode, Devin Desktop) appears to share the store: the V15 comment names the default ACP agent's `session/list` as a reader of the same `sessions` table. No ACP-lane session was captured, so this is unverified.

## Fixture

`packages/pond/tests/fixtures/adapter/devin/` holds two stores, `macos/cli/sessions.db` and `windows/cli/sessions.db`, each directory standing for a `<data>/devin/cli/` root. Captured 2026-09-26 by sandbox self-capture with devin 3000.11.3: five sessions on macOS (headless, tools + resume, a TUI session with a subagent, revert, fork, interrupt and compaction, its fork, and a TUI session with background subagents and a question answer) and two on Windows 11 (a TUI session with tools, a subagent and an interrupt, and a headless one-turn session) - 7 root sessions and 4 subagent children, 11 pond sessions. The capture method, the per-session census and the sweeps are in `packages/pond/tests/fixtures/README.md`.
