# nix-direnv caches the evaluated environment, so entry is instant. Without the
# watch_file line a toolchain bump would go unnoticed until the next flake edit.
watch_file rust-toolchain.toml
use flake
