//! Finding a usable `pond serve` for the desk: this herdr server's published
//! endpoint, else a desk-owned fallback child (plan 5.7), both vetted by the
//! capability probe (plan 5.8). Owner: agent B.
