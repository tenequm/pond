//! The per-herdr-server `pond serve` owner: startup hook and detached
//! watchdog (plan 5.6). Owner: agent B.

pub(crate) fn run(_args: &[String]) -> anyhow::Result<()> {
    anyhow::bail!("not implemented")
}
