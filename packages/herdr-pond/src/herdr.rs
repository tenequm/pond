//! Every herdr CLI call (`pane list`, `agent focus`, `plugin pane open`,
//! `notification show`) and the plugin runtime env (plan 5.2, 5.4). Owner: agent B.

pub(crate) fn open_desk() -> anyhow::Result<()> {
    anyhow::bail!("not implemented")
}
