//! The HTTP [`Api`](crate::types::Api) implementation over `pond serve`
//! (`/v1/x/sql`, `/v1/search`), plus herdr's pane list for live agents.
//! Owner: agent B. Tested against [`crate::fake_pond`].
